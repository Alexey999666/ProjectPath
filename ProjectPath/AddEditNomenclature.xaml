<Window x:Class="ProjectPath.AddEditNomenclature"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Добавление / Редактирование номенклатуры"
        Height="550" Width="600"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Loaded="Window_Loaded">

    <Window.Resources>
        <Style x:Key="InfoCard" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.1"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SectionHeader" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
        </Style>

        <Style x:Key="FormLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
        </Style>

        <Style x:Key="FormTextBox" TargetType="TextBox">
            <Setter Property="Height" Value="35"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="BorderBrush" Value="#D0D0D0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style x:Key="FormComboBox" TargetType="ComboBox">
            <Setter Property="Height" Value="35"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="BorderBrush" Value="#D0D0D0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Background" Value="#C0392B"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="OutlineButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
            <Setter Property="BorderBrush" Value="#BDC3C7"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="6" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <Border Grid.Row="0" Style="{StaticResource InfoCard}" Margin="0,0,0,20">
            <Border.Background>
                <LinearGradientBrush EndPoint="0,1" StartPoint="0,0">
                    <GradientStop Color="#FFFFFF" Offset="0"/>
                    <GradientStop Color="#F8F9FA" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
            <Grid Margin="15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Background="#C0392B" CornerRadius="8" Width="40" Height="40" Margin="0,0,15,0">
                    <TextBlock Text="📦" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>

                <StackPanel Grid.Column="1">
                    <TextBlock x:Name="tbTitle" Text="Добавление номенклатуры" FontSize="18" FontWeight="Bold" Foreground="#2C3E50"/>
                    <TextBlock Text="Заполните все необходимые поля" FontSize="11" Foreground="#7F8C8D"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Форма -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Левая часть - поля ввода -->
            <Border Grid.Column="0" Style="{StaticResource InfoCard}" Margin="0,0,15,0">
                <Border.Background>
                    <LinearGradientBrush EndPoint="0,1" StartPoint="0,0">
                        <GradientStop Color="#FFFFFF" Offset="0"/>
                        <GradientStop Color="#F8F9FA" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
                <Grid Margin="15">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Поле Наименование -->
                    <StackPanel Grid.Row="0" Margin="0,0,0,15">
                        <TextBlock Text="Наименование" Style="{StaticResource FormLabel}"/>
                        <TextBox x:Name="tbName" Style="{StaticResource FormTextBox}"/>
                    </StackPanel>

                    <!-- Поле Тип (ComboBox) -->
                    <StackPanel Grid.Row="1" Margin="0,0,0,15">
                        <TextBlock Text="Тип номенклатуры" Style="{StaticResource FormLabel}"/>
                        <ComboBox x:Name="cbType" Style="{StaticResource FormComboBox}">
                            <ComboBoxItem Content="Деталь"/>
                            <ComboBoxItem Content="Узел"/>
                            <ComboBoxItem Content="Сборочная единица"/>
                            <ComboBoxItem Content="Материал"/>
                            <ComboBoxItem Content="Комплект"/>
                            <ComboBoxItem Content="Инструмент"/>
                            <ComboBoxItem Content="Оснастка"/>
                            <ComboBoxItem Content="Сырьё"/>
                            <ComboBoxItem Content="Изделие"/>
                            <ComboBoxItem Content="Расходники"/>
                        </ComboBox>
                    </StackPanel>

                    <!-- Поле Единица измерения (ComboBox) -->
                    <StackPanel Grid.Row="2" Margin="0,0,0,15">
                        <TextBlock Text="Единица измерения" Style="{StaticResource FormLabel}"/>
                        <ComboBox x:Name="cbUnitMeasure" Style="{StaticResource FormComboBox}">
                            <ComboBoxItem Content="шт"/>
                            <ComboBoxItem Content="кг"/>
                            <ComboBoxItem Content="м"/>
                            <ComboBoxItem Content="л"/>
                            <ComboBoxItem Content="компл"/>
                            <ComboBoxItem Content="уп"/>
                            <ComboBoxItem Content="пара"/>
                            <ComboBoxItem Content="м²"/>
                            <ComboBoxItem Content="м³"/>
                        </ComboBox>
                    </StackPanel>

                    <!-- Пустое место -->
                    <Grid Grid.Row="3"/>

                    <!-- Кнопки -->
                    <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                        <Button x:Name="btnSave" Content="💾 Сохранить" Style="{StaticResource ActionButton}" Width="120" Click="btnSave_Click"/>
                        <Button Content="❌ Отмена" Style="{StaticResource OutlineButton}" Width="120" Click="btnCancel_Click" Margin="10,0,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Правая часть - фото -->
            <Border Grid.Column="1" Style="{StaticResource InfoCard}" Width="200">
                <Border.Background>
                    <LinearGradientBrush EndPoint="0,1" StartPoint="0,0">
                        <GradientStop Color="#FFFFFF" Offset="0"/>
                        <GradientStop Color="#F8F9FA" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
                <Grid Margin="15">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="📷 Изображение" Style="{StaticResource SectionHeader}" TextAlignment="Center"/>

                    <StackPanel Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center">
                        <Border BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="8" Width="150" Height="150" Margin="0,0,0,10">
                            <Image x:Name="imgPhoto" Stretch="Uniform" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                        <TextBlock x:Name="tbPhotoName" Text="Фото не выбрано" FontSize="10" Foreground="#7F8C8D" TextAlignment="Center" Margin="0,5,0,0"/>
                    </StackPanel>

                    <Button x:Name="btnAddPhoto" Grid.Row="2" Content="🖼️ Выбрать фото" Style="{StaticResource OutlineButton}" 
                            Click="btnAddPhoto_Click" Margin="0,10,0,0"/>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>